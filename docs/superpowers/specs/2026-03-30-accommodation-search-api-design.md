# Accommodation Search API — Design Spec

**Date:** 2026-03-30
**Project:** WeSki — Backend Assignment

---

## Overview

A Node.js/TypeScript REST API that serves a ski accommodation search client. The client submits a search, receives an ID, then polls for results. The API fans out to one or more external accommodation providers in the background, aggregating results into an in-memory store. GET requests read only from the store — the provider is never called more than once per search.

---

## Endpoints

### POST /search

Initiates a search. Returns immediately with a search ID. All provider calls happen in the background.

**Request body:**
```json
{
  "ski_site": 4,
  "from_date": "03/04/2025",
  "to_date": "03/11/2025",
  "group_size": 2
}
```

| Field        | Type   | Description                              |
|-------------|--------|------------------------------------------|
| `ski_site`   | number | Resort ID (see resorts reference list)   |
| `from_date`  | string | Start date `MM/DD/YYYY`                  |
| `to_date`    | string | End date `MM/DD/YYYY`                    |
| `group_size` | number | Number of guests                         |

**Response `200`:**
```json
{ "id": "a1b2c3d4-..." }
```

`id` is a UUID generated by this API to track the search job.

**Validation errors → `400`** with `{ "error": "<message>" }`.

---

### GET /search/:id

Returns accumulated results from the store. Never calls the provider.

**Response `200`:**
```json
{
  "id": "a1b2c3d4-...",
  "status": "pending" | "complete",
  "results": [
    {
      "hotelCode": "PWCSTANIN",
      "hotelName": "Chalet Nina",
      "mainImage": "https://...",
      "images": ["https://...", "https://..."],
      "rating": 3,
      "beds": 2,
      "position": {
        "latitude": 45.2969,
        "longitude": 6.5757,
        "distances": [
          { "type": "ski_lift", "distance": "430m" },
          { "type": "city_center", "distance": "230m" }
        ]
      },
      "price": {
        "amountBeforeTax": 263.34,
        "amountAfterTax": 273.34
      }
    }
  ]
}
```

The client polls this endpoint. When `status === "complete"`, all provider calls have finished and no further polling is needed. Partial results are returned while `status === "pending"` — the client can render them immediately.

**Unknown ID → `404`** with `{ "error": "Search not found" }`.

---

## Background Search Flow

```
POST /search
  │
  ├─ Validate request body
  ├─ Compute cache key from (ski_site, from_date, to_date, group_size)
  ├─ If matching search exists in store → return its id (no new requests fired)
  ├─ Generate UUID (search id)
  ├─ Write to store: { id, status: "pending", results: [] }
  ├─ Return { id } to client
  │
  └─ Background (non-blocking):
       For each registered provider:
         Fan out calls for group_size, group_size+1, group_size+2 (max cap: 6)
         ↓ each call resolves independently:
           Normalize results → append to store.results
         ↓ all calls settled:
           Set store.status = "complete"
```

Provider call failures are caught, logged, and treated as empty results. The search still completes with whatever succeeded.

---

## Request Efficiency

The external API has rate limits, so unnecessary calls must be avoided:

**Search deduplication:** The store maintains a secondary index keyed by a cache key derived from `ski_site + from_date + to_date + group_size`. If POST /search receives parameters identical to an existing search (pending or complete), it returns the existing `id` immediately — no new provider calls are made.

**Parallel fan-out:** All group size variants and all providers fire simultaneously via `Promise.allSettled`. No sequential waiting between calls.

**Single fetch per search:** GET /search/:id never calls the provider. Results are written once (at POST time) and read many times.

---

## Group Size Fan-out

The external provider returns results only for the exact group size requested. To offer larger room options, the API queries for `group_size`, `group_size + 1`, and `group_size + 2`, capped at a configurable maximum (default: 6).

All fan-out calls for all providers fire in parallel via `Promise.allSettled`.

Results are appended to the store as each call resolves — not batched at the end.

---

## Provider Abstraction

All providers implement a single interface:

```typescript
interface AccommodationProvider {
  search(params: SearchParams): Promise<Accommodation[]>
}

interface SearchParams {
  ski_site: number
  from_date: string
  to_date: string
  group_size: number
}
```

`HotelsSimulatorProvider` is the only implementation today. Adding a new provider requires only: implement the interface, register the instance in the provider list. No changes to `SearchService` or routes.

The `SearchService` receives the provider list via constructor injection, making it trivially testable with mock providers.

---

## External Provider — HotelsSimulator

**Endpoint:** `POST https://gya7b1xubh.execute-api.eu-west-2.amazonaws.com/default/HotelsSimulator`

**Request:**
```json
{ "query": { "ski_site": 1, "from_date": "03/04/2025", "to_date": "03/11/2025", "group_size": 4 } }
```

**Response shape:**
```json
{
  "statusCode": 200,
  "body": {
    "success": "true",
    "accommodations": [
      {
        "HotelCode": "PWCSTANIN",
        "HotelName": "Chalet Nina",
        "HotelDescriptiveContent": {
          "Images": [
            { "URL": "https://..." },
            { "MainImage": "True", "URL": "https://..." }
          ]
        },
        "HotelInfo": {
          "Position": {
            "Latitude": "45.29690200",
            "Longitude": "6.57577000",
            "Distances": [
              { "type": "ski_lift", "distance": "430m" },
              { "type": "city_center", "distance": "230m" }
            ]
          },
          "Rating": "3",
          "Beds": "2"
        },
        "PricesInfo": {
          "AmountAfterTax": "273.34",
          "AmountBeforeTax": "263.34"
        }
      }
    ]
  }
}
```

The provider returns numeric fields as strings; normalization to numbers happens in the provider implementation before results are stored.

---

## Ski Resorts Reference

Stored locally in the project as `src/data/resorts.json`. No runtime HTTP fetch needed.

```json
[
  { "id": 1, "name": "Val Thorens" },
  { "id": 2, "name": "Courchevel" },
  { "id": 3, "name": "Tignes" },
  { "id": 4, "name": "La Plagne" },
  { "id": 5, "name": "Chamonix" }
]
```

Used at request validation time to verify that `ski_site` is a known resort ID.

---

## In-Memory Store

```typescript
interface SearchRecord {
  id: string
  status: "pending" | "complete"
  results: Accommodation[]
}

// Primary index:   Map<searchId, SearchRecord>
// Secondary index: Map<cacheKey, searchId>
// cacheKey = `${ski_site}:${from_date}:${to_date}:${group_size}`
```

A plain `Map` is sufficient for the assignment scope. No persistence, no TTL. The store is a singleton injected into `SearchService`.

---

## Project Structure

```
src/
  data/
    resorts.json              # Static ski resort list (id + name)
  providers/
    types.ts                  # AccommodationProvider interface + shared types
    hotelsSimulator.ts        # HotelsSimulator provider implementation
  routes/
    search.ts                 # Route handlers for POST /search and GET /search/:id
  services/
    searchService.ts          # Fan-out logic, provider orchestration, store writes
  store/
    searchStore.ts            # In-memory Map wrapper
  app.ts                      # Express app setup, middleware, route registration
  index.ts                    # HTTP server entry point (binds port)
```

---

## Dependencies

| Package       | Purpose                    |
|--------------|----------------------------|
| `express`     | HTTP framework             |
| `uuid`        | Search ID generation       |
| `zod`         | Request body validation    |
| `typescript`  | Already installed          |
| `tsx`         | Dev server / TS execution  |
| `@types/express`, `@types/node` | Type definitions |

---

## Error Handling Summary

| Scenario                        | Response              |
|--------------------------------|-----------------------|
| Missing/invalid request fields | `400 { error: "..." }` |
| Unknown search ID              | `404 { error: "Search not found" }` |
| Provider call failure          | Logged, empty results, search still completes |
| Unhandled server error         | `500 { error: "Internal server error" }` |
